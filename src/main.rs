mod playground;

fn main() {
    // playground::hello_warp::serve();
    // playground::fs::read();
    // playground::json::parse();
    // playground::data_operate::parse();
    playground::guessing_name::start();
}
